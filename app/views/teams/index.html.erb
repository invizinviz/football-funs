
  <% @teams.each do |team| %>
    <ul class='col-sm-2 list-unstyled list-inline btn btn-lg btn-default' id="show-teams">
      <li>
        <%= link_to team_path(team) do %> 
        <%=image_tag "#{team.img}" %>
      </li>
    </ul>
   <% end %>
  <% end %>


