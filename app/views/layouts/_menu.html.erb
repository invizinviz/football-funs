  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to root_path, class: 'navbar-brand logo' do %>
        <%= image_tag ('logo.png') %><span>Football for Fans</span> 
        <% end %>
      </div>
      
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        
        <% @teams.each do |team| %>
          <ul class='main-teams list-unstyled list-inline'>
            <li>
              <%= link_to team_path(team) do %> 
                <%=image_tag "#{team.img}", class: "button hvr-grow-shadow" %> 
         <% end %>
           </li>
         </ul>  
        <% end %>

        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "About", about_path %></li>
          <li><%#= link_to "Scheme", scheme_path %></li>
          <li>
            <% if current_user %>
              Signed as: 
              <%= current_user.name %>
              <%= link_to "Logout", logout_path %>
            <% else %>
              <%= link_to "Login", "/auth/twitter" %>
              <!-- <a href="auth/twitter">Login</a> -->
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </nav>